export const BILLS_SKILL = `
Skill: bill-processing

Когда пользователь просит обработать счета, счет-фактуры, документы на оплату или посчитать итог к оплате:
1) сначала ОБЯЗАТЕЛЬНО вызови инструмент process_bills;
2) передай в process_bills массив paths из запроса пользователя;
3) не рассчитывай суммы вручную, пока не получишь ответ инструмента;
4) после ответа инструмента верни пользователю:
   - итоговую сумму к оплате,
   - путь к сформированному отчету,
   - краткую детализацию по файлам.
5) количество обработанных счетов указывай только по фактическому результату process_bills
   (по количеству элементов в entries), а не по количеству входных путей.

Если paths не переданы или неполные — попроси пользователя уточнить пути.
`;
