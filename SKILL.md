---
name: bill-processing
description: Обработка счетов и счет-фактур через tool `process_bills`. Использовать, когда пользователь просит посчитать итог к оплате по документам (xlsx/xls/pdf/doc/docx) или сформировать отчет.
---

# Bill processing (process_bills)

Когда запрос связан со счетами, счет-фактурами, документами на оплату или итогом к оплате:

- Сначала ОБЯЗАТЕЛЬНО вызывай tool `process_bills`.
- Передавай в `process_bills.paths` путь `docs`.
- Не рассчитывай суммы вручную до получения результата инструмента.

## Правила использования

- В CLI при выборе мода `bills`/`billsWithModel` документы автоматически скачиваются из Яндекс.Диска в папку `docs` (YandexDiskService по API: PUBLIC_URL_DOCS + YANDEX_OAUTH из .env) перед обработкой. Указание путей пользователем не поддерживается.
- Всегда вызывай `process_bills` с путём `docs`.
- Количество обработанных документов указывай только по `entries.length` из результата инструмента.
- Если tool вернул ошибку, покажи ее пользователю кратко и предложи исправить формат документов.
- Не создавай никаких дополнительных файлов/конфигов и тд. кроме итогового отчета bills_report

## Формат финального ответа

После успешного вызова `process_bills` верни:

- итоговую сумму к оплате;
- путь к отчету;
- краткую детализацию по файлам.

Не выдумывай файлы, суммы или путь к отчету. Не дополняй свой ответ ничем кроме того, что указано в формате финального ответа.
